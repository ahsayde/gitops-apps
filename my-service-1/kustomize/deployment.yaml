apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-service-1
  labels:
    app: my-service-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-service-1
  template:
    metadata:
      labels:
        app: my-service-1
    spec:
      containers:
      - name: ubuntu
        image: ubuntu:latest
        command: ["sleep", "100d"]
        securityContext:
          privileged: true